/**
 * @arcanada/publisher-facebook — placeholder.
 *
 * Implementation deferred to a later phase; see the project README for the
 * roadmap. Importing this module currently throws at runtime to make
 * accidental use obvious during foundation work.
 */
export function notImplemented(): never {
  throw new Error(
    "@arcanada/publisher-facebook is a placeholder package; implementation lands in a later phase.",
  );
}
